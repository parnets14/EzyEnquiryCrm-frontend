# CRM frontend API target.
#
# PRODUCTION (Render) — KYC documents/files live here, so document previews work.
VITE_API_URL=https://ezyenquiry-backend.onrender.com/api
#
# LOCAL backend — use this to test local code changes.
# Note: files uploaded on production will NOT open locally (they aren't on your disk).
# VITE_API_URL=http://localhost:5000/api
